{
  "name": "mapgaps-v2",
  "version": "2.0.0",
  "private": true,
  "description": "Dashboard to spot missing, incomplete, contested and inconsistent metadata in Europeana heritage collections.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "sample": "node scripts/generate-sample.js",
    "start": "python3 -m http.server 8000"
  }
}
